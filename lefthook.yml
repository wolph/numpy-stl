# Pre-commit hooks for numpy-stl development
# Install: brew install lefthook && lefthook install
# Or: pip install lefthook && lefthook install

pre-commit:
  parallel: true
  commands:
    ruff-check:
      glob: '*.py'
      run: ruff check {staged_files}
    ruff-format:
      glob: '*.py'
      run: ruff format {staged_files}
      stage_fixed: true
    ty-check:
      run: ty check
